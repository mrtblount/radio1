/**
 * ═══════════════════════════════════════════════════════════════════
 *  EDIT CHANNEL NAMES HERE — this is the only place they're defined.
 *  Add, remove, or rename freely (any number of channels), then deploy:
 *      npx convex deploy -y && vercel --prod --yes
 *  Channels need no backend setup — they exist by being named here.
 * ═══════════════════════════════════════════════════════════════════
 */
export const CHANNELS = ["Security", "Parking Lot", "General"] as const;
